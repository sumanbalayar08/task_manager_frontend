export const AUTH_ENDPOINTS = {
  LOGIN: '/api/login',
  REGISTER: '/api/register',
  LOGOUT: '/api/logout',
  ME: '/api/me',
} as const;

export const TASK_ENDPOINTS = {
  TASKS: '/api/tasks',
} as const;

